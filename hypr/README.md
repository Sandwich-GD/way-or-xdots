## Hyprland

Hyprland is a wayland COMPOSITOR. Call it a window manager, if you're WRONG.
Anyways, this is updated form hyprland.conf to hyprland.lua already, so you're all set.

Ya know the deal, check "example.png" for how it should look.
